# 2020학년도 RA 공식해설 Markdown 수정 보고서

## 1. 입력 파일

- 초안: `converted-solutions_GPTdraft` 해제본 (기존 `official-solutions/2020_solution_files`)
- PDF: `docs_solutions/법전협_2020 LEET 해설_추리논증.pdf` (88p 렌더, 200dpi → `_page_renders/2020`)
- 검수보고서: `converted-solutions_GPTdraft/review-reports/2018-2020_RA_solutions_review_report.md` (2020 = **반려**, OCR 손상 다수 지적)
- 백업: `_backups/2020_solution_files_20260704_210407`

## 2. 처리 결과

- 출력 폴더: `official-solutions/2020_solution_files`
- Markdown 파일: `2020_RA_official+solutions.md`
- 문항 수: 40 (## 01 ~ ## 40, 연속)
- 이미지 파일 수: 0개 (초안 6개는 모두 표 → Markdown/HTML 변환 후 `_unused_images`로 이동, 삭제 아님)
- 최종 판정: **통과** (Q01~Q40 전면 재작성, PDF 육안 대조 완료)

## 3. 주요 수정사항 (OCR 손상 전면 교정)

- 검수보고서가 지적한 대량 OCR 손상(뿌×70, 뼈×14, `\`×49, `@`×65, 오답해설설×11 등)을 **문항별 전면 재작성(splice)** 으로 교정.
- 한자 행위자 기호 전면 복원: `뿌/뼈`→`甲`, `\`→`丙`/`乙`, `류`→`丙`, `며`→`甲` 등 문맥별 대조 후 확정. 등장 인물 `甲乙丙丁戊己`(Q24·Q28·Q30·Q33 등).
- 원문자 `①~⑤`, 원문자 한글 `㉠~㉡`(Q29·Q37), 화학 기호(`Cl⁻`, `HCO₃⁻`, `Zn²⁺`, `Cu²⁺`, `⇌`), 부등호(`≤`, `<`), 전위값(`+0.92V`, `−0.13` 등) 복원.
- 오탈자·손상어 정정: `보소금상한제`→`보조금상한제`, `보혐통계기관`→`보험통계기관`, `옮은`→`옳은` 등.

### 주요 항목별
| 문항 | 내용 | 조치 |
|---:|---|---|
| Q07 | 초안에 `<보기> ㄹ` 누락(PDF는 ㄱㄴㄷㄹ, 정답 ⑤) | 보기 ㄹ 포함 전면 재작성 |
| Q12 | 정답 PDF 대조 확정 = ⑤ | 정답 확정 반영 |
| Q16 | 표1·표2(도덕적 비난 가능 여부, rowspan/colspan) | 이미지 → HTML `<table>` 변환 |
| Q28 | 교환 방식 표 | 이미지 → Markdown 표 |
| Q31 | 표1·표2(채점 결과 9경우 / 성적) | 이미지 → Markdown 표 |
| Q40 | 표준환원전위 순서표(D/B/A/C) | 이미지 → Markdown 표 |
| Q38 | 실험 `<결과>` 표는 **문제(제시문) 측** 자료로 해설 본문에 미포함(초안·해설 범위와 일치) | 표 미삽입 |

## 4. 표 처리

- HTML 표 2개(Q16 표1·표2, rowspan/colspan), Markdown 표 4개(Q28, Q31 표1·표2, Q40). 모두 PDF와 값 대조 완료.
- 빈칸 `.` 플레이스홀더 0건 (Q31 표1의 `성적` 열 빈칸은 원문대로 빈 셀 유지).

## 5. 이미지 처리

- 초안 이미지 6개(16_1·16_2, 28_1, 31_1·31_2, 40_1)는 **전부 표 이미지** → Markdown/HTML 표로 대체.
- 6개 모두 `2020_solution_files/_unused_images/`로 이동(삭제하지 않음). 본문 이미지 참조 0건.
- 순수 도식(다이어그램) 이미지는 없음 → 최종 이미지 참조 0.

## 6. 남은 위험 항목

- **Q33 선택지 ⑤: PDF 원문 자체 내부 불일치.** 결정 서열은 `'甲戊己乙'`로 서술되나 뒤이은 가능 서열은 `'丙丁戊甲己乙' 또는 '丁丙戊甲己乙'`(=`戊甲己乙`)로 인쇄됨. 논리상 `甲戊`가 옳으나 **원문대로 전사**. (p072, 고해상 crop 재확인) — 정답(②)에는 영향 없음. 상세: [remaining_risk_items](remaining_risk_items.md).
- 정답 표기는 PDF 원문과 동일하게 원문자 `①~⑤` 사용(2025·2026의 `(n)` 표기와 상이 — 연도 간 표기 통일 여부는 사용자 판단 필요).

## 7. 자동 검증 결과

- 문항 헤딩: 40개 연속(## 01~## 40) + 출제방향 4개
- 문항구분 40 / 정답 40 / 문항 성격 40 / 평가 목표 40
- OCR 아티팩트(뿌·뼈·류은·며의·보소금·`\`·`@`·`ㅠ`·`100;` 등): 0
- 표: HTML 2 + Markdown 4, 빈칸 `.` 0
- 이미지 참조 0(전부 표 변환), `_unused_images` 6개 보관
- 정답 분포: ①×7 ②×8 ③×9 ④×7 ⑤×9
- 정답 PDF 대조(전 문항 육안): 예) Q22=① p047, Q25=④ p055, Q33=④ p071, Q40=② p087
- UTF-8(BOM 없음), 원본 입력 폴더(`converted-solutions_GPTdraft`, `docs_solutions`) 무수정
